_schema-version: "3.2"
ID: zstudent71
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: zstudent71-destination-content
  type: com.sap.application.content
  requires:
  - name: zstudent71-destination-service
    parameters:
      content-target: true
  - name: zstudent71-repo-host
    parameters:
      service-key:
        name: zstudent71-repo-host-key
  - name: zstudent71-uaa
    parameters:
      service-key:
        name: zstudent71-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: zstudent71_html_repo_host
          ServiceInstanceName: zstudent71-html5-service
          ServiceKeyName: zstudent71-repo-host-key
          sap.cloud.service: zstudent71
        - Authentication: OAuth2UserTokenExchange
          Name: zstudent71_uaa
          ServiceInstanceName: zstudent71-xsuaa-service
          ServiceKeyName: zstudent71-uaa-key
          sap.cloud.service: zstudent71
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: zstudent71-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: zstudent71-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - zstudent71.zip
      name: zstudent71
      target-path: resources/
- name: zstudent71
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: zstudent71-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: zstudent71-destination-service
    service-plan: lite
- name: zstudent71-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: zstudent71-xsuaa-service
    service-plan: application
- name: zstudent71-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: zstudent71-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
